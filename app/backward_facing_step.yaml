problem: backward_facing_step_Re=200
mesh: "../inputs/backward_facing_step_8450_elementos.msh"

parameters:
  viscosity: 0.0025
  density: 1.0

boundaries:
  u:
    - { type: DIRICHLET, region: TOP, value: zero }
    - { type: DIRICHLET, region: BOTTOM, value: zero }
    - { type: DIRICHLET, region: LEFT, value: bfs }
    - { type: NEUMANN, region: RIGHT, value: zero }
    - { type: DIRICHLET, region: STEP_HORIZONTAL, value: zero }
    - { type: DIRICHLET, region: BACKWARD_VERTICAL, value: zero }

  v:
    - { type: DIRICHLET, region: TOP, value: zero }
    - { type: DIRICHLET, region: BOTTOM, value: zero }
    - { type: DIRICHLET, region: LEFT, value: zero }
    - { type: NEUMANN, region: RIGHT, value: zero }
    - { type: DIRICHLET, region: STEP_HORIZONTAL, value: zero }
    - { type: DIRICHLET, region: STEP_VERTICAL, value: zero }

  p:
    - { type: NEUMANN, region: TOP, value: zero }
    - { type: NEUMANN, region: BOTTOM, value: zero }
    - { type: NEUMANN, region: LEFT, value: zero }
    - { type: DIRICHLET, region: RIGHT, value: zero }
    - { type: NEUMANN, region: STEP_HORIZONTAL, value: zero }
    - { type: NEUMANN, region: STEP_VERTICAL, value: zero }

solver: steady

simple:
  lambda_uv: 1e-3
  lambda_p: 1e-3
  iterations: 100

export_folder: "../outputs/"